from setuptools import setup, find_packages

setup(
    name='$project',
    description='',
    version='',
    author='',
    license='BSD',
    url='',
    packages = find_packages(),
    dependency_links = [
#for $app in $required_apps
    #if 'find_links' in $required_apps[$app].keys
        #for $link in $required_apps[$app]['find_links']
        '$link',
        #end for
    #end if
#end for
    ],
    install_requires = [
#for $app in $required_apps
    '$app',
#end for
    ],
)
