from django.db import models

#for $app in $app_config
    #if $getVar($app) == 'y'
        #if 'model_additions' in $app_config[$app].keys
            #include $app_config[$app]['model_additions']
        #end if
    #end if
#end for
